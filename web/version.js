// The app version, shared with the desktop app — keep in lockstep with
// src/bio_overlay/__init__.py and pyproject.toml (a unit test enforces the
// match). Bump all three together when cutting a release.
export const VERSION = "1.5.0";
